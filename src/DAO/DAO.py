import os
from pathlib import Path
from dotenv import load_dotenv
"""
	DAO: Data Access Object
	This file handles all queries to the sqlite3 databae
"""

class DAO:
	

	# Here's where database queries should go

	def insert_training_data(data):
		pass